#include <stdio.h>
#define MAXLINE 100

int getLine(char line[], int maxline);
void reverse(s);

/* print input in reverse line by line */
main()
{
  char line[MAXLINE];
  char reversed[MAXLINE];

  while (getLine(line, MAXLENGTH) > 0){

  }
}
